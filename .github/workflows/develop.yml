# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ master ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v1
        
      - name: Set up Docker Build
        uses: docker/setup-buildx-action@v1
        
      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          
      - name: get nt-app metadata
        id: nt_app
        uses: docker/metadata-action@v3
        with:
          images: ${{ secrets.DOCKERHUB_IMAGE }}
          
      - name: build nt app and push to registry
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKERHUB_IMAGE }}:develop
          labels: ${{ steps.nt_app.outputs.labels }}
#       - name: CURL-ACTION
#         # You may pin to the exact commit or the version.
#         uses: enflo/curl-action@v1.2
#         with:
#           # curl arguments
#           curl: -d "update=true" -X POST https://docker-dev.yellow-idea.com/api/webhooks/2897444d-ac4d-4afe-bdc7-48081b77bac4
